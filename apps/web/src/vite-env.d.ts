/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts), sourced from package.json.
// Falls back to "dev" when the version is the un-released "0.0.0" placeholder.
declare const __APP_VERSION__: string;
